FROM ros:humble
RUN apt-get update && \
    apt-get install -y --no-install-recommends ros-gz-sim \
    && apt-get clean && rm -rf /var/lib/apt/lists/* 
RUN . /opt/ros/humble/setup.sh 
RUN ls -l /opt/ros/humble/share/ros_gz_sim || echo notfound 